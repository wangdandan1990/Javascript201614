1.关于运动和定时器的优化技巧：
1）在开启定时器前，先关闭没用的定时器
2）边界值的边界值判断；if(curL+step>=target) curL=target;
3）定时器越小越不准确
4）
2.JS中分成同步和异步：
同步：当前任务没做完，不会开始下个任务；--JS中大多都是同步：
比如：循环
异步：当前任务没做完，不会等待，继续开始下个任务；如果后面的任务没有完成，永远不会回过头看前面任务是否完成；
3.异步：
1）事件
2）定时器
3）回调函数
4）ajax
